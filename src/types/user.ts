export type TLogin = {
  username: string;
  password: string;
  currentPassword: string;
  newPassword: string;
  repeatPassword: string;
}

export type TUser = {
  id: string;
  username: string;
}
